version: v2
managed:
  enabled: true
plugins:
  # Generate TypeScript types with ts-proto (compatible with @grpc/grpc-js)
  - remote: buf.build/community/stephenh-ts-proto
    out: generated/ts
    opt:
      - esModuleInterop=true
      - outputServices=generic-definitions
      - useExactTypes=false

