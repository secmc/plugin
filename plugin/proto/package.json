{
  "name": "@dragonfly/proto",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "generate": "buf generate && cd ../.. && go mod tidy",
    "lint": "buf lint",
    "breaking": "buf breaking --against '.git#branch=main'"
  },
  "devDependencies": {
    "@bufbuild/buf": "^1.47.2"
  }
}

